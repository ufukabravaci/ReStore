import { Typography } from "@mui/material";

export default function CheckoutPage() {
    return (
        <Typography variant="h3" >
            Only logged in users should be ablo to see this!
        </Typography>
    )
}